{
  "additionalProperties": false,
  "properties": {
    "result": {
      "items": {
        "additionalProperties": false,
        "properties": {
          "key": {
            "type": "string"
          },
          "value": {
            "description": "MUST be a valid JSON value serialized as a string. This represents a path-item-or-reference reference that could not be resolved. Do NOT output plain text — the value must parse with JSON.parse().",
            "type": "string"
          }
        },
        "required": [
          "key",
          "value"
        ],
        "type": "object"
      },
      "type": "array"
    }
  },
  "required": [
    "result"
  ],
  "type": "object"
}
