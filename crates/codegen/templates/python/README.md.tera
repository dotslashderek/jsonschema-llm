# {{ package_name }} SDK

Generated by [json-schema-llm](https://github.com/dotslashderek/json-schema-llm) `gen-sdk`.

## Components

| Component | Module |
|---|---|
{% for component in components -%}
| `{{ component.component_name }}` | `{{ component.module_name }}` |
{% endfor %}

## Usage

### Roundtrip generation (recommended)

```python
from {{ import_name }} import {{ components.0.module_name }}
from json_schema_llm_engine import LlmRoundtripEngine, OpenAIFormatter, ProviderConfig, HttpTransport

engine = LlmRoundtripEngine()
formatter = OpenAIFormatter()
config = ProviderConfig(
    url="https://api.openai.com/v1/chat/completions",
    model="gpt-4o",
    headers={"Authorization": f"Bearer {api_key}"},
)
transport = HttpTransport()

result = {{ components.0.module_name }}.generate(
    "Generate a {{ components.0.component_name }}",
    engine=engine,
    formatter=formatter,
    config=config,
    transport=transport,
)
print(result.data)
```

### Low-level access

```python
from {{ import_name }} import {{ components.0.module_name }}

# Access individual artifacts if needed
schema   = {{ components.0.module_name }}.schema()
codec    = {{ components.0.module_name }}.codec()
original = {{ components.0.module_name }}.original()
```

## Install

```bash
pip install -e .
```
