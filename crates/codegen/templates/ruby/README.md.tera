# {{ sdk_name }}

Auto-generated Ruby SDK for json-schema-llm schemas.

## Installation

Add to your Gemfile:

```ruby
gem "{{ sdk_name }}"
```

Then run:

```bash
bundle install
```

## Usage

### Unified generation (recommended for generic operations)

Use `generate` and `from_name` for dynamic dispatch — ideal when the target component is determined at runtime.

```ruby
require "{{ module_name }}"
require "json_schema_llm_engine"

engine = JsonSchemaLlmEngine::LlmRoundtripEngine.new(
  formatter: JsonSchemaLlmEngine::Formatters::ChatCompletions.new,
  config: JsonSchemaLlmEngine::ProviderConfig.new(
    url: "https://api.openai.com/v1/chat/completions",
    model: "gpt-4o",
    headers: { "Authorization" => "Bearer #{ENV["OPENAI_API_KEY"]}" }
  ),
  transport: my_transport
)

# Generate any component by name
result = {{ generator_module }}.generate("{{ components.0.name }}", "Your prompt here", engine)

# Available component constants:
{% for component in components %}# {{ generator_module }}::{{ component.enum_name }} => "{{ component.name }}"
{% endfor %}
```

### Focused component access

Access a specific component module directly when you know exactly which component you need.

```ruby
{% for component in components %}# {{ component.name }}
{{ generator_module }}::{{ component.module_name }}.generate("Your prompt here", engine)
{{ generator_module }}::{{ component.module_name }}.schema    # LLM-compatible schema
{{ generator_module }}::{{ component.module_name }}.codec     # Rehydration codec
{{ generator_module }}::{{ component.module_name }}.original  # Original JSON Schema
{% endfor %}
```

## Components

{% for component in components %}
### {{ component.name }}

| Constant | `{{ generator_module }}::{{ component.enum_name }}` |
|---|---|
| Module | `{{ generator_module }}::{{ component.module_name }}` |
{% endfor %}

## License

Generated by [json-schema-llm](https://github.com/dotslashderek/jsonschema-llm).
