# {{ package_name }} SDK

Generated by [json-schema-llm](https://github.com/dotslashderek/json-schema-llm) `gen-sdk`.

## Components

| Component | Schema | Codec |
|---|---|---|
{% for component in components -%}
| `{{ component.component_name }}` | `{{ component.class_name }}.schema()` | `{{ component.class_name }}.codec()` |
{% endfor %}

## Usage

```java
import {{ package_name }}.SchemaGenerator;
import com.jsonschema.llm.engine.*;

// Load a component schema
var schema = SchemaGenerator.info_schema();

// Use with any provider formatter
var formatter = new ChatCompletionsFormatter();
var config = new ProviderConfig("https://api.openai.com/v1/chat/completions", "gpt-4o", headers);
var request = formatter.format("Generate an API info block", schema, config);
```

## Build

```bash
mvn compile
```
