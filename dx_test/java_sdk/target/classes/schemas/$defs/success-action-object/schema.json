{
  "$comment": "https://spec.openapis.org/arazzo/v1.0#success-action-object",
  "additionalProperties": false,
  "description": "A single success action which describes an action to take upon success of a workflow step",
  "patternProperties": {
    "^x-": {
      "description": "MUST be a valid JSON object serialized as a string, e.g. \"{\\\"key\\\": \\\"value\\\"}\". Do NOT output plain text — the value must parse with JSON.parse().",
      "type": "string"
    }
  },
  "properties": {
    "criteria": {
      "anyOf": [
        {
          "description": "A list of assertions to determine if this action SHALL be executed. Constraints: items must be unique; minimum 1 item(s).",
          "items": {
            "$comment": "https://spec.openapis.org/arazzo/v1.0#specification-extensions",
            "description": "While the Arazzo Specification tries to accommodate most use cases, additional data can be added to extend the specification at certain points",
            "patternProperties": {
              "^x-": {
                "description": "MUST be a valid JSON object serialized as a string, e.g. \"{\\\"key\\\": \\\"value\\\"}\". Do NOT output plain text — the value must parse with JSON.parse().",
                "type": "string"
              }
            }
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ]
    },
    "name": {
      "description": "The name of the success action",
      "type": "string"
    },
    "stepId": {
      "anyOf": [
        {
          "$anchor": "stepId",
          "description": "Unique string to represent the step",
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "type": {
      "description": "The type of action to take",
      "enum": [
        "end",
        "goto"
      ]
    },
    "workflowId": {
      "anyOf": [
        {
          "$anchor": "workflowId",
          "description": "Unique string to represent the workflow",
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "criteria",
    "name",
    "stepId",
    "type",
    "workflowId"
  ],
  "type": "object"
}
